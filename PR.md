# The frontier arcs concluded: an exact localization law and the phase-number locking law

## Summary

The two named remaining arcs of the mass and quantum programmes,
taken to measured conclusions in one branch. Arc one (quantitative
settled-gradient localization): the frozen-gradient instrument
found a position-independent EXACT localization law (the at-rest
species pins at exactly zero displacement, locked, at every seed
position), exposed the earlier deep-dive as a configuration
transient, and found no deep ladder, closing the last measured
candidate for a dynamical mass-hierarchy origin: the hierarchy
magnitude is a free input exactly as the Standard Model holds it,
reached by measurement. Arc two (the single-quantum coherence
bridge): the phase-knob search found the PHASE-NUMBER LOCKING law
instead (no slab regime moves a dressed packet's phase at fixed
number, blind regimes exact, modified amplitudes quantized exactly
at two root three, root thirty-nine, two root twenty-one), so
substrate-level single-quantum coherence does not exist in this
regime and the number-organized visibility floor stands as the
committed observable.

## What changed

- foundations/frozen-gradient-localization (E-FND-0139): the exact
  pin at all swept positions, the bounded drifter, the unlocalized
  massless control.
- foundations/phase-number-locking (E-FND-0140): the locking law
  with exact blind controls and the quantized amplitude ladder.
- note/open/frontier-arcs.md: both arcs' complete stories including
  the transient the sweep caught.
- readme.md: the two-arcs hit added to What it found, and the
  open-research paragraph replaced by the planted-stakes paragraph.
- task/: frozen-gradient-map.ts, phase-knob-sweep.ts (permanent).

## How it was tested

Typecheck clean. Both experiments' verification runs and their
seventeen-position and twelve-offset probe sweeps are recorded in
tmp logs and the task scripts. The full suite was NOT rerun on this
branch (fast-iteration rule); the main tree's post-merge suite run
covers the base.

## Honest status

- Both conclusions are negative-with-mechanism results and say so:
  no hierarchy ladder (the model does not derive the hierarchy),
  no phase knob (the model forbids substrate single-quantum
  coherence in this regime), each with its committed falsifiable
  observable.
- The locking is measured at one geometry and one species, scoped
  in the experiment.

## Follow-ups

- The main-tree catalog sync (task/sync-catalog.ts) after merge for
  the two new codes, scoreboard regen, suite run.
- The two planted stakes are external-facing and wait on nature:
  the number-organized error floor in quantum hardware, and the
  graphene channel profile.
