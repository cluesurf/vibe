# Findings 4, Physics, Particles and Forces

The matter content and the interactions, every result, with the mechanism and why it matters. Companion to
`findings.md`. Cite `\cite{pollard2026vibetest}`, with the octonion-standard-model line (Furey, Baez, Dixon) noted
where it converges.

## Spin, the electron from a finite table

The 24 sites split under triality as `8v + 8s + 8c`, a vector and two spinors, and only this substrate carries
spinors at all, the `{5,3,4}` dodecagrid's 12 directions split `1 + 3 + 3' + 5` with NO spinor (golden-ratio,
non-crystallographic). Under a 2pi rotation the spinor overlap is `cos(theta/2)` with period 4pi while the vector
overlap is `cos(theta)` with period 2pi, so `2pi = -1` and `4pi = +1` exactly, in a finite group. Why it matters,
only the right substrate can hold an electron, and spin-half lives with no continuum.

## The Dirac equation read off the mesh

The Clifford algebra closes on the mesh, `{gamma_mu, gamma_nu} = 2 eta_munu` (the Minkowski metric appears), and the
relativistic energy `H^2 = m^2 + |p|^2` is READ OUT of the dynamics, not assumed. The massless dispersion is `E = k`
exactly, the massive one `cos E = cos m cos k`, the lattice form that reduces to `E^2 = m^2 + k^2`. Why it matters,
the equation governing every fermion in nature emerges from a tone hopping on a crystal.

## Chirality without doublers, the lattice's hardest problem

Putting a single chiral fermion on a lattice is notoriously blocked by the fermion-doubling theorem (naive lattices
give 4 unwanted copies, Wilson's fix breaks chirality by about 1e-3). The overlap Dirac operator here gives ONE
species with a Ginsparg-Wilson residual under 1e-9, exactly chiral. The Kahler-Dirac construction (`D = d + delta`,
with `D^2` the Hodge Laplacian) carries a fermion even through the 12 spinless directions of other substrates (a
clean return of 0.05 versus 0.4 disordered), which is why matter is generic across all 42 tessellations. Why it
matters, the model solves the single problem every discrete approach to matter is supposed to fail.

## Mass, measured two ways and emergent

Mass is computed two independent ways that agree, from the dispersion gap and from the chirality coupling
`[H, gamma5]`, and it VANISHES at zero coupling (the massless Weyl control). The mass comes from coupling the two
chiralities 8s and 8c, turn the coupling off and the fermion is a massless Weyl particle. Why it matters, mass is not
inserted by hand, it is a measured consequence of the 8s-8c coupling, the lattice version of the Higgs mechanism.

## Topology of the spinor, zero modes count holes

The number of spinor zero modes equals the topological complexity, disk 1, cylinder 2, torus 4, matching the Betti
number sum (the count of independent holes), with the spectrum symmetric about zero. A half-winding disclination (a
wedge defect) gives the spinor a holonomy of `(-1)^w` for ANY discretization while a vector gets `+1`, and a
continuum Gauss-Bonnet loop of area 2pi flips the spinor's sign. Why it matters, the deepest index theorems of
geometry hold on the discrete mesh, the spinor genuinely feels topology.

## Three generations, the forced count and the open gap

The threefold structure is real, the D4 long roots are the 24 roots of F4, the exceptional Jordan algebra J3(O)
(dimension 27) is forced to rank 3, and an exact S3 permutes its three primitive slots. But the three slots come out
DEGENERATE (identical trace, rank, and norm), so a mass-splitting into three distinct families is UNPROVEN, this is
Boyle's conjecture. The naive reading of `8v + 8s + 8c` is a vector plus two chiralities of ONE generation, not three
families. Honesty, the count three is geometrically forced, why the three differ in mass is the program's boldest
open question. Why it matters, the most stubborn number in particle physics is structurally present, and its origin
is the cleanest stated unknown.

## Discrete symmetries and matter-antimatter

Parity holds (D4 is closed under negation and coordinate flips), time-reversal holds (the Dirac walk recovers its
start to 1e-12), and the create move makes matter and antimatter together (a `+1/-1` pair from peace), so a self plus
its antiself annihilate to vacuum. The CPT pieces are present (tone-flip for charge conjugation, handed-swap for
parity, beat-reverse for time), measured consistent to 1e-15, though a formal CPT theorem is still open. Opposite
defects annihilate to near-zero energy, like-charge defects persist, winding conserved. Why it matters, the discrete
symmetries that organize the particle table fall out of the dock's geometry, and the existence of matter (over
nothing) traces to one create move.

## Grand unification is the smallest fix the geometry allows

The gauge group is forced, not chosen. The D4 directions plus the tone axis make D5, which is SO(10), the standard
model embeds in it cleanly with the 16-spinor equal to `8s + 8c`, and the standard model does NOT fit in SO(8), it
REQUIRES the SO(10) that the tone supplies. The breaking SO(10) to SU(5) goes through the 16-spinor singlet with all
16 weights preserving the 20 roots, no fine-tuning. The octonion route to the standard model (Furey, Baez, Dixon)
lands in the same place. Why it matters, grand unification is not an added assumption, it is the smallest group the
substrate's own structure demands.

## The Weinberg angle, a real number from a tone

At unification `sin^2(theta_W) = 3/8`, the textbook grand-unified value, which runs down to about 0.231 at the Z
mass, the measured value. Why it matters, a genuine number of particle physics, recovered from a tone on a dock,
candidate evidence the embedding is right and not a coincidence.

## Mass relations and running

`Tr Y = 0` (hypercharges sum to zero) forces `det(M_lepton) = det(M_down)` (the Georgi-Jarlskog relation), and the
bottom-tau mass ratio runs from 1 at unification to about 2.3 at the Z mass under QCD running, against the observed
2.35. The Higgs is a self-condensate. Why it matters, relations between the masses of different particles, long known
phenomenologically, come out of the same structure.

## Charge quantization from anomaly cancellation

Requiring the gauge anomalies to cancel forces the standard model hypercharges to UNIQUE values (`Y_Q = 1/6`,
`Y_u = -2/3`, and the rest), so charge is quantized exactly as observed. Why it matters, the puzzle of why the proton
and electron charges match to extreme precision is answered by consistency, not coincidence.

## The photon and the gauge fields

The 8v octet is a massless U(1) photon, dispersion `omega = 2|sin(k/2)|`, gapless and linear, with 2 transverse
polarizations and about 33 percent gauge (pure-gauge) zero modes, and the Wilson action flows to Maxwell (ratio to
1). The Wilson loop equals the enclosed flux, gauge-invariant, with an exact Aharonov-Bohm phase. The non-abelian
case carries SU(2), the commutator `[sigma_i, sigma_j] = 2i eps sigma_k` measured. A 1D QED runs with charge
conserved to 1e-9, Gauss's law `div E = rho`, a Peierls phase, and back-reaction (the charge accelerates at the
Newtonian rate `dp/dt = eE`, decoupling exactly at `e = 0`). Why it matters, electromagnetism, the first field
theory, with its gauge invariance and Aharonov-Bohm effect, is a mode of the mesh.

## Confinement, why a free quark is never seen

The SU(2) Creutz ratio gives a positive string tension at every coupling, and a 1D gauge field confines opposite
charges at ANY energy with a constant force (the Gauss-law flux tube). Why it matters, quark confinement, the reason
isolated quarks do not exist, emerges from the same gauge structure.

## The electron's magnetic moment, not put in by hand

The g-factor measures to `2.0 +/- 0.05` from the Dirac Landau spectrum (across 2 fields and 2 masses), where a scalar
control has no such zero mode. Why it matters, the electron's measured magnetic strength, one of physics' precision
triumphs, comes out at the Dirac value with nothing inserted.

## The index theorem and the open coupling

The overlap operator's zero-mode count equals the gauge topological charge (the Atiyah-Singer index on the mesh). The
running couplings of the bare standard model MISS unification, while the supersymmetric three couplings meet (within
`Delta alpha < 1`) near 1e16 GeV. The coupling VALUE is free, a clean `e^2` law that vanishes only at `e = 0` with no
distinguished point, so the rule fixes the FORM of the dependence, not the number, the 1/137 problem stays open. The
charge-2 soliton has about twice the charge-1 mass (additivity), the Skyrme stabilizer sign comes out POSITIVE so a
3D stabilization needs the Skyrme term (2D is marginal), and the second-photon-field route was abandoned (no viable
competitor to the single 8v photon). Why it matters, the model recovers the structure of unification and the
deepest index theorems, while honestly marking the fine-structure constant as not yet derived.

## Where to look (code and experiments)

- Spin and Dirac: `spin/spinor-triality`, `spin/rotation-2pi`, `spin/sp1-spin-double-cover`, `spin/dirac-3plus1-3434`,
  `spin/dirac-from-discrete`, `spin/chirality`, `spin/kahler-dirac-534`, `spin/measured-emergent-mass-3434`,
  `spin/topology` (zero modes), `spin/disclination`, `spin/continuum-holonomy`, `spin/sy-discrete-symmetries`,
  `spin/defect-particles-3434`. Generations: `spin/generations-f4-jordan`, `spin/generation-family-symmetry-3434`,
  `spin/triality-generations`. Algebra in `code/algebra/octonion.ts`, `code/algebra/jordan.ts`.
- Forces: `gauge/gauge-from-coin-tone`, `gauge/gauge-embedding`, `gauge/electroweak-prediction`, `gauge/gut-breaking`,
  `gauge/vacuum-selection`, `gauge/mass-relations`, `gauge/yukawa-rg`, `gauge/anomaly-charge-quantization`,
  `gauge/ph-photon-3434`, `gauge/photon`, `gauge/gauge-from-action`, `gauge/emergent-u1-gauge`,
  `gauge/nonabelian-gauge`, `gauge/non-abelian-3434`, `gauge/coupled-qed-3434`, `gauge/coemergence-dynamical-3434`,
  `gauge/confinement`, `gauge/two-charge-binding`, `gauge/g-factor-3434`, `gauge/index-theorem`,
  `gauge/rg-unification`, `gauge/coupling-not-fixed-3434`, `gauge/soliton-matter`, `gauge/skyrme-sign`,
  `gauge/higgs`. Operators in `code/operator/landau.ts`, gauge code in `code/dynamics/schwinger-coupled.ts`,
  `code/tool/gauge-field.ts`.