# Origin Data

The documentation for these CSVs has moved to the conceptual notes:
`../../../../note/research/vibe/notes/origin-data.md`.

This folder holds the raw CSV datasets (one per tiling, plus `growth-summary.csv`).
